## Anomaly detector.py
# anomaly_detector.py
# anomaly_detector.py
import pandas as pd

def detect_anomalies(df: pd.DataFrame) -> pd.DataFrame:
    """
    Input: dataframe with patient vitals/features
    Output: same dataframe with:
      - anomaly_score (float)
      - is_flagged (bool)
    """
    pass